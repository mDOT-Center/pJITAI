upstream webapp {
    ip_hash;
    server 0.0.0.0:5001;
}

server {
    listen 80;
    server_name pjitai.mdotcenter.org;

    location / {
        proxy_pass http://webapp;
        proxy_set_header Host $host;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }

}
